{
  "name": "samaara-jewellery",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm ci --prefix frontend && npm run build --prefix frontend && npm ci --omit=dev --prefix backend",
    "start": "npm start --prefix backend"
  },
  "engines": {
    "node": ">=20 <25"
  }
}
